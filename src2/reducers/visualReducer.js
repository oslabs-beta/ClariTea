import { defaultInputTarget } from 'concurrently/src/defaults';
import * as types from '../constants/actionTypes';

const initialState = {

};
const visualrReducer = (state = initialState, action) => {
  
}
export default visualReducer;